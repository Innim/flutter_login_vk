name: flutter_login_vk_example
description: Demonstrates how to use the flutter_login_vk plugin.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_login_vk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  innim_lint: ^0.1.0

flutter:
  uses-material-design: true
